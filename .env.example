# Optional frontend build setting. Blank uses the browser's timezone.
# Copy to .env.local, edit, and rebuild after changing it.
NEXT_PUBLIC_TIME_ZONE=

# Python does not load .env files. Export runtime settings in your shell:
# export KERNEL_INBOX_PORT=8765
# export KERNEL_INBOX_CACHE_DIR="$HOME/.cache/kernel-inbox"
# export KERNEL_INBOX_THEME=default
